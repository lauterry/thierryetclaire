angular.module('ThierryEtClaire').controller('rsvpController', function ($scope) {
    "use strict";

	$scope.contact = {
		nombreAdultes : 1,
		nombreEnfants : 0
	};

	$scope.sendResponse = function () {

	};

});
